<configuration>
  
  <appender name="PLAIN_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg [application=${APP-NAME}, environment=${APP-ENV}, %X]%n</pattern>
    </encoder>
  </appender>

  <root level="INFO">
    <appender-ref ref="PLAIN_CONSOLE" />
  </root>

</configuration>
